export enum RewardSource {
    LOGIN = 'LOGIN',
    TASK = 'TASK',
    STREAK = 'STREAK',
    MILESTONE = 'MILESTONE'
}

export enum RewardType {
    ZIG = 'ZIG',
    FAUCET = 'FAUCET'
}

export enum FaucetStatus {
    PENDING = 'PENDING',
    SENT = 'SENT',
    FAILED = 'FAILED'
}
